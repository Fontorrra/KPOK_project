spring:
  datasource:
    url: jdbc:tc:postgresql:15:///appointment-db
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create